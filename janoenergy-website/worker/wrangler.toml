name = "janoenergy-api"
main = "src/index.ts"
compatibility_date = "2024-02-08"

[vars]
JWT_SECRET = "janoenergy-secret-key-2026"

# 自定义域名路由
routes = [
  { pattern = "api.janoenergy.com", custom_domain = true }
]

# 生产环境变量需要在 Cloudflare Dashboard 中设置
# DATABASE_URL = "postgresql://..."
